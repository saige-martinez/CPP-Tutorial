/*
 * This program gets user input, stores the input in the variable x, and
 * outputs the input.
 */

#include <iostream>
using namespace std;

int main() {
    int x;
    cout << "Enter a number: ";
    cin >> x;
    cout << "You entered: " << x;
    return 0;
}
